name: Deploy website

on:
  push:
    branches: [ develop ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Setup Git
      run: |
        git config --global user.name 'chowson'
        git config --global user.email 'chowson@users.noreply.github.com'
    
    - name: Install and Build 🔧 
      run: |
        npm install
        npm run deploy