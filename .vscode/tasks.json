{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Install",
      "command": "yarn",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "type": "shell",
      "label": "Build",
      "command": "yarn build",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "type": "shell",
      "label": "Develop",
      "command": "yarn develop",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
